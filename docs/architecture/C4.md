# Architecture Map — Maxwell_Daemon

This document defines the canonical Mermaid C4 architecture map for `Maxwell_Daemon`, providing verified context, container boundaries, capability-to-component traceability, and architecture change history.

## Context View (C4Context)

```mermaid
C4Context
    title System Context diagram for Maxwell_Daemon

    Person(developer, "Developer / Operator", "Directs autonomous software engineering tasks via web dashboard or CLI.")
    System(maxwell_daemon, "Maxwell_Daemon", "Autonomous engineering team in a box orchestrating planning, implementation, and adversarial validation.")
    System_Ext(runner_dashboard, "Runner Dashboard", "Fleet operator console consuming daemon HTTP API and WebSocket event streams.")
    System_Ext(repo_mgmt, "Repository_Management", "Fleet governance, policy sync, and multi-agent coordination contracts.")
    System_Ext(target_repos, "Managed Repositories", "Codebases inspected, modified, tested, and pushed by Maxwell worker tasks.")
    System_Ext(byo_cli, "External LLM Backends / CLIs", "Anthropic Claude, OpenAI, Ollama, or local CLI agents executing prompt turns.")

    Rel(developer, maxwell_daemon, "Inspects tasks and controls runs", "HTTP / UI / WebSocket")
    Rel(runner_dashboard, maxwell_daemon, "Monitors status and fleet worker tasks", "HTTP API / WebSocket")
    Rel(repo_mgmt, maxwell_daemon, "Establishes fleet standards and lease rules", "Git / Documentation")
    Rel(maxwell_daemon, target_repos, "Clones, edits files, executes tests, and opens PRs", "Git / Shell / gh CLI")
    Rel(maxwell_daemon, byo_cli, "Dispatches agent prompts and cognitive pipeline steps", "CLI / REST API")
```

## Container View (C4Container)

```mermaid
C4Container
    title Container diagram for Maxwell_Daemon

    Person(user, "User / Operator", "Interacts via browser or API.")

    Container_Boundary(daemon_boundary, "Maxwell_Daemon Service") {
        Container(web_ui, "Web Dashboard", "Vanilla JS / HTML / CSS", "Browser dashboard at /ui/ for task control, cost graphs, and gate views.")
        Container(api_gateway, "FastAPI Gateway", "Python (maxwell_daemon/api/)", "REST and WebSocket endpoints under /api/v1/*.")
        Container(daemon_core, "Daemon & Task Queue", "Python (maxwell_daemon/daemon/)", "Background worker loop, concurrency limits, and thread-safe submission.")
        Container(cognitive_pipeline, "Cognitive Pipeline", "Python (maxwell_daemon/director/)", "Strategist (planning), Implementer (coding), and Crucible (adversarial testing).")
        Container(execution_engine, "Execution & Sandbox", "Python (maxwell_daemon/sandbox/)", "Policy-gated executor, argv allowlist, and timeout guards.")
        Container(backend_adapters, "LLM Backends", "Python (maxwell_daemon/backends/)", "Adapters for Claude, OpenAI, and BYO-CLI providers.")
        Container(event_bus, "Event Bus & Cost Ledger", "Python (maxwell_daemon/events.py)", "Typed in-process event streaming and SQLite token cost tracking.")
        Container(fleet_coord, "Fleet Coordinator", "Python (maxwell_daemon/fleet/)", "Multi-repo management, worker registration, and heartbeat monitors.")
    }

    System_Ext(local_fs, "Local File System", "Working directories, git checkouts, and configuration files.")

    Rel(user, web_ui, "Navigates UI", "HTTP")
    Rel(web_ui, api_gateway, "Calls REST / WebSocket", "JSON / WS")
    Rel(api_gateway, daemon_core, "Enqueues tasks", "Python")
    Rel(daemon_core, cognitive_pipeline, "Executes workflow steps", "Python")
    Rel(cognitive_pipeline, backend_adapters, "Sends prompt turns", "Python")
    Rel(cognitive_pipeline, execution_engine, "Runs bash/pytest actions", "Python")
    Rel(execution_engine, local_fs, "Modifies files & runs subprocesses", "OS Subprocess")
    Rel(daemon_core, event_bus, "Emits task lifecycle events", "Python")
    Rel(daemon_core, fleet_coord, "Dispatches fleet-level work", "Python")
```

## Feature Map

| Capability | Component | Interface | Evidence |
| --- | --- | --- | --- |
| Web Dashboard Console | `maxwell_daemon.api.ui` | Browser `/ui/`, gate-aware status | `tests/unit/test_api_status.py` |
| REST API & OpenAPI Contract | `maxwell_daemon.api` | `/api/v1/*`, FastAPI router | `tests/unit/test_openapi_docs_sync.py` |
| Thread-Safe Task Queue | `maxwell_daemon.daemon` | `submit_threadsafe()`, stall cancellation | `tests/unit/test_daemon.py` |
| Cognitive Pipeline Roles | `maxwell_daemon.director` | Strategist, Implementer, Crucible state machine | `tests/unit/test_director.py` |
| Policy-Gated Execution | `maxwell_daemon.sandbox` | `argv` allowlist, workspace boundaries | `tests/unit/test_sandbox.py` |
| Backend CLI Adapters | `maxwell_daemon.backends` | Claude, OpenAI, BYO-CLI adapters | `tests/unit/test_backends.py` |
| Typed Event Streaming | `maxwell_daemon.events` | `EventBus`, `/api/v1/events` WebSocket | `tests/unit/test_events.py` |
| Fleet Topology & Dispatch | `maxwell_daemon.fleet` | `fleet.yaml` manifest, worker registration | `tests/unit/test_fleet.py` |
| Architecture Map Contract | `scripts.architecture_map_contract` | `validate_architecture_map()` | `tests/scripts/test_architecture_map_contract.py` |

## Architecture Change Log

| Date | PR / Issue | Description |
| --- | --- | --- |
| 2026-09-10 | #1601 | Adopt maintainable Mermaid C4 architecture-map contract and automated CI verification |
