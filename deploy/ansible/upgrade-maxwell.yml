---
# upgrade-maxwell.yml — Rolling upgrade of maxwell-daemon across the fleet.
#
# Upgrades agents first, then the primary controller.
# Each node is health-checked before the playbook proceeds to the next.
#
# Usage:
#   ansible-playbook -i inventory/fleet.yml upgrade-maxwell.yml \
#     -e maxwell_version=0.2.0
#
# Variables (may be set in inventory or on the command line):
#   maxwell_venv     (default: /opt/maxwell-daemon/venv)
#   maxwell_version  (default: latest)
#   maxwell_port     (default: 8080)

- name: Set standalone upgrade defaults
  hosts: maxwell_agents:maxwell_primary
  gather_facts: false
  tasks:
    - name: Set default variables for standalone playbook runs
      ansible.builtin.set_fact:
        maxwell_venv: "{{ maxwell_venv | default('/opt/maxwell-daemon/venv') }}"
        maxwell_version: "{{ maxwell_version | default('latest') }}"
        maxwell_port: "{{ maxwell_port | default(8080) }}"

- name: Rolling upgrade — agent nodes
  hosts: maxwell_agents
  become: true
  gather_facts: false
  serial: 1   # one node at a time

  tasks:
    - name: Stop service before upgrade
      ansible.builtin.systemd:
        name: maxwell-daemon
        state: stopped

    - name: Upgrade maxwell-daemon package
      ansible.builtin.pip:
        name: >-
          maxwell-daemon{{ '[all]' if maxwell_version == 'latest' else '[all]==' + maxwell_version }}
        virtualenv: "{{ maxwell_venv }}"
        state: "{{ 'latest' if maxwell_version == 'latest' else 'present' }}"

    - name: Start service after upgrade
      ansible.builtin.systemd:
        name: maxwell-daemon
        state: started

    - name: Wait for health endpoint
      ansible.builtin.uri:
        url: "http://127.0.0.1:{{ maxwell_port }}/healthz"
        status_code: 200
        timeout: 5
      retries: 12
      delay: 5
      register: health_check
      until: health_check.status == 200

- name: Rolling upgrade — primary controller
  hosts: maxwell_primary
  become: true
  gather_facts: false

  tasks:
    - name: Stop primary controller
      ansible.builtin.systemd:
        name: maxwell-daemon
        state: stopped

    - name: Upgrade maxwell-daemon package
      ansible.builtin.pip:
        name: >-
          maxwell-daemon{{ '[all]' if maxwell_version == 'latest' else '[all]==' + maxwell_version }}
        virtualenv: "{{ maxwell_venv }}"
        state: "{{ 'latest' if maxwell_version == 'latest' else 'present' }}"

    - name: Start primary controller
      ansible.builtin.systemd:
        name: maxwell-daemon
        state: started

    - name: Wait for health endpoint
      ansible.builtin.uri:
        url: "http://127.0.0.1:{{ maxwell_port }}/healthz"
        status_code: 200
        timeout: 5
      retries: 12
      delay: 5
      register: health_check
      until: health_check.status == 200

    - name: Confirm upgraded version
      ansible.builtin.command:
        cmd: "{{ maxwell_venv }}/bin/maxwell-daemon --version"
      register: version_out
      changed_when: false

    - name: Show new version
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} running {{ version_out.stdout }}"
